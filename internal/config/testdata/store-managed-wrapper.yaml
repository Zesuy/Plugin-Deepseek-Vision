plugins:
  enabled: true
  store:
    refresh_interval: 1h
  configs:
    deepseek-vision:
      enabled: true
      priority: 100
      store:
        repository: example/deepseek-vision
        version: v0.1.0
        managed: true
      vision_model: store-managed-model
